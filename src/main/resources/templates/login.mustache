<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title>Spring Security Example </title>
</head>
<style>
    header{
        width: 100%;
        background: #222222;
        height: 62px;
    }
    .nft-header {
        width: 120px;
        cursor: pointer;
    }
    *{
        margin: 0;
        padding: 0;
    }
    body{
        background: #191919;
    }

    a{
        text-decoration: none;
    }

    h2{
        width: 200px;
        position: relative;
        left: 180px;
        top: 50%;
        transform: translate(-50%, -50%);
        color: #6A5ACD;
        text-decoration: none;
    }
    h1{

        position: absolute;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
        color: white;
        text-decoration: none;
    }
    .container-login {
       position: absolute;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
        color: white;
        text-decoration: none;
    }
    input[type=text] {
        border-radius: 7px;
        width: 100%;
        padding: 20px 20px;
        margin: 8px 0;
        box-sizing: border-box;
    }
    input[type=password] {
        border-radius: 7px;
        width: 100%;
        padding: 20px 20px;
        margin: 8px 0;
        box-sizing: border-box;
    }
    button[type="submit"] {
        border: none;
        border-radius: 7px;
        padding: 15px 83px;
        background: #6A5ACD;
        cursor: pointer;
        text-transform: uppercase;
        font-weight: bold;
        color: white;
        transition: 1s;
        margin: 10px 0px;
      }
        button[type="submit"]:hover {
        background: #483D8B;
      }
      button[name="addNY"] {
        border: 2px;
        border-radius: 7px;
        border-color: #000000;
        padding: 15px 55px;
        background: white;
        cursor: pointer;
        text-transform: uppercase;
        font-weight: bold;
        color: white;
        transition: 1s;
      }
        button[name="addNY"]:hover {
        background: #A9A9A9;
      }
</style>
<header>
<!--    <img src="../NFT.png" class="nft-header">-->
    <a href="/main"> <h2>SocialClub </h2> </a>

</header>
<body>

<form action="/login" method="post">
    <div class="container-login">

        <div><label> <input type="text" name="username" required placeholder="Username:"/> </label></div>
        <div><label><input type="password" name="password" required placeholder="Password: "/> </label></div>
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <div><button type="submit" value="Sign In">Sign in</button></div>
        <div><button type="submit" name="addNY"><a href="/registration"> Add new user </a></button></div>
    </div>
</form>
</body>
</html>
